# Models

This directory is reserved for model and artifact storage used by the production inference workflow.

Binary model artifacts are not manually committed to Git.

During Task 3, model and artifact versioning will be managed using DVC and MLflow.
