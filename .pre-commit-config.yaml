repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.8
    hooks:
      - id: ruff-check
        types_or: [python, pyi]
        exclude: ^scripts/

      - id: ruff-format
        args: [--check]
        types_or: [python, pyi]
        exclude: ^scripts/
